type Query {
  job(id: ID!): Job
  jobs: [Job]
}

type Job {
  id: ID!
  date: String!
  title: String!
  company: Company!
  description: String
}

type Company {
  id: ID!
  name: String!
  description: String
}
